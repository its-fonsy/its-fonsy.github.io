# Autocompile with `latexmk`

`latexmk` command for autocompiling without stopping

    latexmk -pdf -pvc -verbose -file-line-error -interaction=nonstopmod

copied a little bit from the plugin vimtex. Need to read the documentation of
`latexmk`.
